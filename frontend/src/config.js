/* --- File: frontend/src/config.js --- */
const config = {
    // This is the URL for your deployed backend server.
    API_BASE_URL: 'https://resumetojobai-3.onrender.com',
};

export default config;
